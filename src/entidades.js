const estudiante = { cedula, nombre, correo, telefono }

const curso = { id, nombre, descripcion, valor, modalidad, duracion, activo } 

const matricula = { idCurso, cedulaEstudiante }

module.exports = {estudiante,curso,matricula};
